# 游戏配置列表优化 - 检查清单

**创建时间**: 2026-03-25
**相关规格**: spec.md
**相关任务**: tasks.md

---

## 开发检查清单

### 预检

- [ ] 已阅读 spec.md 设计文档
- [ ] 已理解需求：时间筛选、状态缓存、回车查询、状态记忆、默认排序

### 代码实现

#### 状态变量
- [ ] `searchTimeRange` ref 定义
- [ ] `TIME_RANGE_OPTIONS` 常量定义
- [ ] `ONLY_MINE_KEY` 常量定义
- [ ] `FILTER_STATE_KEY` 常量定义

#### 函数实现
- [ ] `filterByTimeRange()` 时间范围过滤
- [ ] `saveFilterState()` 保存筛选状态
- [ ] `loadFilterState()` 恢复筛选状态
- [ ] `sortGamesByEditTime()` 排序函数

#### 生命周期
- [ ] `onMounted` 中恢复 `onlyMine` 状态
- [ ] `onMounted` 中恢复筛选状态
- [ ] `watch` 监听 `onlyMine` 变化
- [ ] `watch` 监听筛选条件变化

#### 事件绑定
- [ ] `searchGameId` 输入框回车事件
- [ ] `searchGameName` 输入框回车事件

#### UI 组件
- [ ] 时间筛选下拉框模板
- [ ] 时间筛选下拉框样式

### 功能测试

#### 时间筛选
- [ ] 选择"今天"，列表只显示今天创建的游戏
- [ ] 选择"近7天"，列表只显示7天内创建的游戏
- [ ] 选择"近30天"，列表只显示30天内创建的游戏
- [ ] 选择"本月"，列表只显示本月创建的游戏
- [ ] 选择"全部"，列表显示所有游戏

#### 仅看我的
- [ ] 勾选"仅看我的"，列表只显示当前用户创建/编辑的游戏
- [ ] 刷新页面后，勾选状态保持
- [ ] 取消勾选后刷新页面，勾选状态不保持

#### 回车查询
- [ ] 在ID输入框按回车，触发查询
- [ ] 在名称输入框按回车，触发查询

#### 筛选状态记忆
- [ ] 输入ID后刷新页面，ID输入框内容保持
- [ ] 输入名称后刷新页面，名称输入框内容保持
- [ ] 选择时间筛选后刷新页面，时间筛选选择保持

#### 默认排序
- [ ] 列表默认按编辑时间倒序排列
- [ ] 最新编辑的游戏排在最前面

---

## 完成后自检

- [ ] 代码符合项目编码规范
- [ ] 函数有适当的注释
- [ ] 无 console.log 调试代码残留
- [ ] 样式符合 design_rule.md 规范
