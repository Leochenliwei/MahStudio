# 将编辑器视图改为弹窗模式实现计划

## 1. 模板结构修改
- 将 `editor-view` 部分从 `content-area` 中移出来，创建独立的弹窗容器
- 在弹窗容器外添加遮罩层，实现模态效果
- 保留原有的仪表盘视图结构不变

## 2. 响应式数据添加
- 添加新的响应式变量 `showEditorModal` 控制弹窗显示/隐藏
- 保持现有的 `viewMode` 变量用于内部逻辑判断

## 3. 方法逻辑调整
- 修改 `handleCardClick` 方法：点击规则卡片时显示编辑器弹窗
- 修改 `createNew` 方法：创建新规则时直接显示编辑器弹窗
- 修改 `backToDashboard` 方法：关闭编辑器弹窗并重置状态
- 修改 `saveAndClose` 方法：保存规则后关闭编辑器弹窗
- 修改 `confirmEdit` 方法：确认编辑后显示编辑器弹窗

## 4. 样式调整
- 添加弹窗容器样式：固定定位、居中显示、最大宽度/高度
- 添加遮罩层样式：半透明背景、模糊效果
- 调整弹窗内部布局：确保编辑器内容在弹窗中正确显示
- 添加弹窗动画效果：平滑的显示/隐藏过渡

## 5. 功能验证
- 测试规则编辑功能是否正常
- 测试新建规则功能是否正常
- 测试弹窗的显示/隐藏动画是否流畅
- 测试响应式布局是否适配不同屏幕尺寸

## 6. 注意事项
- 保持现有功能不变，确保修改后所有操作正常
- 确保弹窗能够正确接收和处理数据
- 保持代码风格与现有代码一致
- 添加必要的注释说明修改内容